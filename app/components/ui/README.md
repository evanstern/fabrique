# shadcn/ui base components

This directory is reserved for generated shadcn/ui base components.
Keep these files as close to the generated upstream output as practical.
Do not make Fabrique-specific product customizations directly in these base files.

Put product compositions, wrappers, and expanded variants in `app/components/fabrique/`.
